import React, { FC } from "react";

import { Layout } from "../components/Layout.js";

export const UserFilters: FC = () => {
    return (
        <Layout>
            <h1>User Filters</h1>
        </Layout>
    );
};
