import React, { FC } from "react";

import { Layout } from "../components/Layout.js";

export const Environments: FC = () => {
    return (
        <Layout>
            <h1>Environments</h1>
        </Layout>
    );
};
